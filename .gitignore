# build output
server/build/
build/
build-*/
cmake-build-*/
*.dSYM/

# OS / editor junk
.DS_Store
.vscode/
.idea/
*.swp

# saved matches written by `w2f_server --autosave`
*.w2fsave

__pycache__/
*.pyc

# the designer's splash art copies (their own art: keep out of the public repo unless they decide otherwise)
server/docs/icons/T_Splash_*.png
splash_arts/

# Mixamo downloads (~2 GB of FBX; Mixamo's licence allows them in a game but not redistributed as raw files in a public repo)
server/docs/examples/
server/docs/animations/
